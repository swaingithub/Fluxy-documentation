---
title: One Engine. Infinite Possibilities.
description: Fluxy is a "Full-Stack" Flutter platform (v0.2.0) that unifies high-performance networking, architectural authority, and reactive state management.
---

# Fluxy v0.2.0
**Professional Inspector & Architectural Authority.**

<div className="grid grid-cols-1 md:grid-cols-3 gap-4 my-6">
  <div>
    <h4 className="text-sm font-bold mb-2 uppercase tracking-widest text-muted-foreground">1. Install CLI</h4>
    ```bash
    dart pub global activate fluxy
    ```
  </div>
  <div>
    <h4 className="text-sm font-bold mb-2 uppercase tracking-widest text-muted-foreground">2. New Project</h4>
    ```bash
    fluxy init my_awesome_app
    ```
  </div>
  <div>
    <h4 className="text-sm font-bold mb-2 uppercase tracking-widest text-muted-foreground">3. Existing Project</h4>
    ```bash
    flutter pub add fluxy
    ```
  </div>
</div>

Fluxy is a "Full-Stack" Flutter platform designed to modernize mobile and web development. It unifies high-performance **Native Networking**, **Architectural Authority**, and the **Fluxy State System** into a single, cohesive engine for building enterprise-grade applications at scale.

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-8">
  <div className="p-4 border rounded-lg bg-card text-card-foreground">
    <h3 className="text-xl font-bold mb-2">Professional Inspector</h3>
    <p>A premium glassmorphic HUD for real-time DI Registry inspection, network activity logs, and state update timelines.</p>
  </div>
  <div className="p-4 border rounded-lg bg-card text-card-foreground">
    <h3 className="text-xl font-bold mb-2">Architectural Authority</h3>
    <p>Standardized patterns with Scoped DI, FluxController (lifecycle hooks), and Fluxy Blueprints for scalable apps.</p>
  </div>
  <div className="p-4 border rounded-lg bg-card text-card-foreground">
    <h3 className="text-xl font-bold mb-2">Fluxy State System</h3>
    <p>Enterprise-grade reactive state with fluxSelector, fluxWorker, and automatic hydration. Zero jank, infinite scalability.</p>
  </div>
  <div className="p-4 border rounded-lg bg-card text-card-foreground">
    <h3 className="text-xl font-bold mb-2">CLI Intelligence</h3>
    <p>Generate complete features with `fluxy g`, scaffold plugins/models/layouts, and run local OTA servers.</p>
  </div>
</div>

## The Fluxy Philosophy

Fluxy isn't just a state management library; it's an **all-in-one Flutter ecosystem**. 

Traditional Flutter development often requires stitching together multiple packages: one for state, one for networking, and one for styling. Fluxy provides all of these as first-class citizens, integrated into a single **Reactive Engine** that minimizes boilerplate and maximizes flow.

### v0.2.0 Highlights - Architectural Authority

- **Fluxy Inspector**: Glassmorphic HUD with real-time system mirrors.
- **Scoped DI (`FxScope`)**: Structured lifecycles (`app`, `route`, `factory`) with auto-cleanup.
- **Global Error Pipeline**: Capture all application and platform errors via `Fx.onError`.
- **Modular Plugin System**: Built-in interface for extending the framework logic.
- **Enhanced CLI**: New blueprints for `plugin`, `layout`, `model`, and `controller`.

### v0.1.11 Highlights - Routing & DI Stabilization

- **Automatic Route DI**: `FxRoute` now handles controller registration and disposal automatically.
- **Enhanced DI Engine**: Better internal framework orchestration with `putByRuntimeType`.
- **Blueprint Polish**: Reverted property getters back to methods for stable DSL chaining.


### v0.1.8 Highlights - The Fluxy State System

- **Branded State Units**: `Flux<T>`, `FluxComputed<T>`, `FluxEffect` - conflict-free professional keywords
- **Targeted Rebuilds**: `fluxSelector` prevents unnecessary UI updates for atomic performance
- **Background Processing**: `fluxWorker` runs heavy computations in isolates for zero-jank UI
- **Auto-Cleanup**: `FluxyLocalMixin` prevents memory leaks with automatic state disposal
- **Offline-First**: `FluxyRepository` with multi-user scoping and automatic state hydration
- **Global Middleware**: Intercept all state changes for analytics and logging
- **Time Travel**: Built-in undo/redo with `fluxHistory`
- **100% Backward Compatible**: All existing Signal code continues to work

### v0.1.7 Highlights

- **Atomic Interaction API**: Expressive buttonizers for strings and widgets (`"Save".primaryBtn()`).
- **Visual FX Engine**: Native modifiers for `.blur()`, `.grayscale()`, and `.circle()`.
- **Chainable Transitions**: Smooth interpolation of styles via `.transition(Duration)`.
- **Sequence DSL**: Unified `.stagger()` animations for rows and columns.
- **Enhanced Motion**: Looping and repeating animations for attention-grabbing effects.

### v0.1.6 Highlights

- **Declarative Conditionals**: Render UI states cleaner with `Fx.cond()`.
- **Intelligent Spacing**: Independent gap widgets (`Fx.gap`, `Fx.hgap`) for precise layouts.
- **Enhanced Inputs**: Multi-function validators and better formatting for `Fx.input`.
- **Next-Gen Layouts**: Explicit `Fx.row`, `Fx.col`, and `Fx.stack` with standard parameters.

## Getting Started

Ready to build your first Fluxy app?

<Cards>
  <Card href="/docs/quick-start" title="Quick Start" description="Install Fluxy CLI and create your first project in under 5 minutes." />
  <Card href="/docs/state-management" title="Fluxy State System" description="Learn about enterprise-grade reactive state management." />
  <Card href="/docs/ui-dsl" title="Atomic Styling DSL" description="Learn how to build beautiful UIs with chainable modifiers." />
</Cards>
